export const THEME_CONFIG = {
    // The default theme is light, but we can change it to dark
    // by setting this to "dark"
    DEFAULT_THEME: "dark",
    // The default color is gold (FFA611), but we can change it
    DEFAULT_COLOR: "#FFA611",
    // The default font is Nunito, but we can change it to
    // another font by changing this value
    DEFAULT_FONT: "Nunito",
};
